<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--头像属性-->
    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width" format="dimension"/>
        <attr name="civ_border_color" format="color"/>
        <attr name="civ_border_overlay" format="boolean"/>
        <attr name="civ_fill_color" format="color"/>
        <attr name="civ_shape" format="enum">
            <enum  name="rect" value="1"/>
            <enum  name="circle" value="2"/>
        </attr>
        <attr name="civ_round_radius" format="dimension"/>
    </declare-styleable>
    <!--顶部菜单-->
    <declare-styleable name="HeaderBar">
        <attr name="isShowBack" format="boolean"/>
        <attr name="titleText" format="string"/>
        <attr name="rightText" format="string"/>
        <attr name="isShowRightText" format="boolean"/>
        <attr name="rightBtn" format="reference"/>
        <attr name="isShowRightBtn" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="CropImageView">
        <attr name="cropMaskColor" format="color"/>
        <attr name="cropBorderColor" format="color"/>
        <attr name="cropBorderWidth" format="dimension"/>
        <attr name="cropFocusWidth" format="dimension"/>
        <attr name="cropFocusHeight" format="dimension"/>
        <attr name="cropStyle" format="enum">
            <enum name="rectangle" value="0"/>
            <enum name="circle" value="1"/>
        </attr>
    </declare-styleable>

    <!--上传照片-->
    <declare-styleable name="ImageShowPickerView">

        <!-- 单张图片大小（宽高一样） -->
        <attr name="pic_size" format="dimension" />
        <!-- 最大数量 -->
        <attr name="max_num" format="integer" />
        <!-- 添加图片 -->
        <attr name="add_label" format="reference" />
        <!-- 删除图片 -->
        <attr name="del_label" format="reference" />
        <!-- 是否显示删除按钮-->
        <attr name="is_show_del" format="boolean" />
        <!-- 是否展示动画-->
        <attr name="is_show_anim" format="boolean" />
        <!-- 单行显示数量-->
        <attr name="one_line_show_num" format="integer" />

    </declare-styleable>

</resources>